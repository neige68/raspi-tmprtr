# .env のテンプレート。cp dot.env .env して編集する。

# サーバーの POST エンドポイント URL
SERVER_URL=https://example.com/sensor_data

# TOTP シークレット（server 側の .env と同じ値を設定する）
TOTP_SECRET=

# センサーが使えない開発環境では 1 に設定するとスタブ値を返す
# raspi 実機では設定しないか 0 にする
MOCK_SENSORS=0
